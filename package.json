{
  "name": "qommercehub",
  "version": "1.0.0",
  "description": "QommerceHub Enterprise E-Commerce Platform",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install",
    "test": "cd backend && npm test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
